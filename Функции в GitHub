Git HUB
1.  создание копии репозитория, с которым планируем работать 
 git clone https://github.com/al-ti/gittraining.git (ссылку берем на сайте Git HUB  кнопкой clone and downloa

2. проверяем статус данного файла,действительноли у нас последняя версия 
git status

3. если наша версия не последняя, нужноскачать последнюю, применяем, также применяем перед началом работы с кодом
git pull 

4. проверяем,какие изменения мы вносим
git diff

5. добавляем измененный нами файл
git add ____

6. если решаем не внедрять наши изменения, применяем
git reset

7. добавляем актуальные комментарии к нашим изменениям 
 git commit "____" 

8. внедряем наши изменения в общее пользование
 git push


